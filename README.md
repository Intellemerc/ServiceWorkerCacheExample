# ServiceWorkerCacheExample

## To Run:

You need two command prompts(One for server and one for client)

### Cmd Prompt 1:

```bash
Yarn Install && node server.js
```

### Cmd Prompt 2:

```bash
Yarn Install && Yarn start
```
